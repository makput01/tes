plugins {
    id 'com.android.application' version '8.1.0'
}

repositories {
    google()
    mavenCentral()
    gradlePluginPortal()
}

android {
    compileSdkVersion = 30
    defaultConfig {
        compileSdkVersion = 30
	namespace = 'com.tes'
	applicationId = 'com.tes'
    }
}
