buildscript {

    repositories {
        google()
        jcenter()
    }

}

allprojects {
    repositories {
        google()
        jcenter()
	gradlePluginPortal()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
